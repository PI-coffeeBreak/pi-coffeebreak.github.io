---
const { src = '/coffeebeans.png' } = Astro.props
---

<section class="hero p-16 sm:py-24 px-6 sm:px-12">
  <div class="container mx-auto">
    <div class="grid grid-cols-1 lg:grid-cols-2 items-center gap-10">
      <!-- Texto -->
      <div class="flex flex-col items-center text-center lg:items-start lg:text-left space-y-6">
        <h1 class="text-4xl sm:text-5xl lg:text-7xl font-bold leading-tight">
          <slot>There's no event without <span class="text-primary">coffeeBreak.</span></slot>
        </h1>
        <p class="text-lg sm:text-xl max-w-md opacity-75">
          An event management platform that allows customization with the modules your event needs.
        </p>
      </div>
      
      <!-- Imagem -->
      <div class="flex justify-center lg:justify-end">
        <img class="w-3/4 sm:w-1/2 lg:w-2/3 max-w-xs lg:max-w-md" src={src} alt="Coffee beans" decoding="async" />
      </div>
    </div>
  </div>
</section>

